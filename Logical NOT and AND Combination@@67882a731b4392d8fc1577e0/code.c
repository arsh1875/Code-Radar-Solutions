#include <stdio.h>

int main() {
    int a, b;
    scanf("%d %d", &a, &b);
    
    // Check if neither number is greater than zero
    if  {
        printf("True\n");
    } else {
        printf("False\n");
    }
    
    return 0;
}
