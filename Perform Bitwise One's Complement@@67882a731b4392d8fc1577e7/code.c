#include <stdio.h>

int main() {
    int a,onesc;
    scanf("%d", &a);
    onesc=~a;
    printf("%d\n",onesc);
    // Check if two numbers have opposite signs using logical and relational operators

    return 0;
}
